const { connection } = require('./connector')
const { data } = require('./data')

const refreshAll = async () => {
    // await connection.deleteMany({})
    // await connection.getAll().then()
    // console.log(connection)
    // await connection.insertMany(data)
}
refreshAll()